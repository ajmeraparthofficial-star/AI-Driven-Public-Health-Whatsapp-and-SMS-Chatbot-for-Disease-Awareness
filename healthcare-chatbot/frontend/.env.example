# Copy this file to `.env` in the frontend folder or set the variables in your deployment.
# Variables must be prefixed with VITE_ to be available to Vite at build/runtime.

VITE_API_URL=http://localhost:5000/api
VITE_GEMINI_KEY=your_gemini_api_key_here
